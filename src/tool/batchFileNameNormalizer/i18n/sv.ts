import { createContent } from './shared';

export const content = createContent({ slug: 'batch-file-name-normalizer', title: 'Byt namn på flera filer', description: 'Byt namn på lokala filer med sök och ersätt, prefix, suffix och numrering. Granska resultaten och ladda ner ZIP kopior.', locale: 'sv' });
