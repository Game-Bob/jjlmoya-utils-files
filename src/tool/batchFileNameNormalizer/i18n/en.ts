import { createContent } from './shared';

export const content = createContent({ slug: 'batch-file-name-normalizer', title: 'Batch File Name Normalizer', description: 'Rename a group of local files with find and replace rules, prefixes, suffixes, and numbering. Preview every result, catch collisions, and download a ZIP without changing the original files.', locale: 'en' });
